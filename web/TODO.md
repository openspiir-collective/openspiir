# Web TODO

Work through these tasks one PR at a time. Each PR should be small, focused, and independently reviewable.

## Foundation

- [ ] **1. Add Tailwind CSS** — install and configure Tailwind CSS v4; replace any placeholder styles with utility classes
- [ ] **2. Enable React Compiler** — add the React Compiler babel plugin and verify it runs on the dev and build pipelines
- [ ] **3. Add Relay** — install Relay, configure the compiler, wire up a `RelayEnvironment` pointing at the backend GraphQL endpoint
- [ ] **4. Set up mock pages** — scaffold placeholder route components for the main app sections (Dashboard, Transactions, Budget, Savings); add client-side routing with react-router
